import { FilterByNameEmailPipe } from './filter-by-name-email.pipe';

describe('FilterByNameEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNameEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
